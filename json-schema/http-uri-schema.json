{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "http-uri-schema.json",
    "description": "Used to validate @id or url is a http or https URI",
    "type": "string",
    "format": "regex",
    "pattern": "^http[s]?://.+"
}